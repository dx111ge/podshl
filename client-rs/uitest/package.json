{
  "name": "podshl-uitest",
  "private": true,
  "type": "module",
  "description": "Drives the real PODSHL window. Deliberately outside client-rs/ui, which Tauri bundles into the app.",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "test:decisions": "node --test \"tests/flow-decisions.test.mjs\""
  },
  "engines": {
    "node": ">=20"
  }
}
