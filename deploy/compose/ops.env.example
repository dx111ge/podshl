# Copy to `ops.env` beside compose.yaml. Read by the ops view and nothing else.
#
# The bearer token every route on :8726 requires. Unset, it answers 503 to
# everything.
#   openssl rand -base64 33
PODSHL_OPS_TOKEN=
